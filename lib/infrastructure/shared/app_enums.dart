enum AppType { client, pro }

enum BackendReachability {
  unknown,
  reachable,
  unreachable,
}

enum AccountStatus {
  unknown,
  ok,
  unauthorized,
  locked,
}

enum MessageType { success, error, warning, info }
